[Unit]
Description=FastAPI Camera Server
After=network.target

[Service]
User=jhoni 
WorkingDirectory=/home/jhoni/Desktop/Carro_remoto
ExecStart=/usr/bin/uvicorn Server.camera_server:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
